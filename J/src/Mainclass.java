import java.util.*; 

import java.lang.*;
public class Mainclass {
	public static void main(String arg[])
	{
		/*
		Scanner scan=new Scanner(System.in);
		MySpecialLinkedListUtils method= new MySpecialLinkedListUtils();
		Node head=new Node(5);
		Node j=head;
		for(int i=7;i>=0;i--)
		{
			j.next=new Node(i);
			j=j.next;
		}
		//head=method.mergesort(head);
		double[] summary =new double [5];
		summary=method.summary(head);
		for(int i=0;i<5;i++)
		{
			System.out.println(summary[i]);
		}
		method.show(head);

		*/
	}
	
}
